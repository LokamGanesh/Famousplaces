{
  "name": "famousplaces",
  "private": true,
  "scripts": {
    "postinstall": "cd frontend && npm install",
    "build": "cd frontend && npx vite build",
    "dev": "cd frontend && npm run dev"
  }
}
